@model IEnumerable<string>
@foreach(string controller in Model)
{
    <a class="btn btn-block @(controller==ViewBag.Controller?"btn-primary":"btn-secondary")"
       asp-action="Index" asp-controller="@controller">
    @controller
    </a>

}
